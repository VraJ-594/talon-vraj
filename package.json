{
  "name": "talon-ats",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "build:web": "npm --workspace @talon/web run build",
    "dev:web": "npm --workspace @talon/web run dev",
    "format:web": "npm --workspace @talon/web run format",
    "format:check:web": "npm --workspace @talon/web run format:check",
    "lint:web": "npm --workspace @talon/web run lint",
    "test:web": "npm --workspace @talon/web run test"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
